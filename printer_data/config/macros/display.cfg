[display_data _default_16x4 layers]
position: 2, 0
text: 
    {% if 'virtual_sdcard' in printer and printer.virtual_sdcard.progress %}
        {(printer.print_stats.info.current_layer)}
        /
        {(printer.print_stats.info.total_layer)}
    {% endif %}